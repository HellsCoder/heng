const routes_dom = [
    {
        selector: '.button',
        type: 'click',
        callback: () => {
          /* Функция будет вызвана при нажатии на объект с классом .button */
        }
    }
];

module.exports = routes_dom;
