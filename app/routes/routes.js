
const routes = [
    {
        link: '/',
        callback: () => {
          console.info("Эта функция будет выполнена когда пользователь перейдет по адресу /");
        }
    }
];

module.exports = routes;
